# Validation notes — 2026-09-09

- Local page compiled and returned HTTP 200. Browser preview opened at the development server URL. Broad browser UI QA was not requested and was not performed.
- TypeScript checked with `npx tsc --noEmit`.
- Scenic-road and driving-effort addition: 8 mapped items (6 approximate window-view corridors, 1 optional parking entrance, 1 parking/transfer entrance). `node scripts/check-road-experience.mjs` passes all 4 endpoint/overnight combinations, exact parking anchors, no navigation for window markers, continuous selected-route highlights up to 5 km either side, an off-route guard, provenance, and input immutability. D6 follows the actual selected return leg. No routes were added or changed.
- `node scripts/check-driving-effort.mjs` performs 16 real React server renders and passes 9 groups of assertions: D1–D7 and overnight variants, 498/486/390-minute D6 baselines, D5 walking/transfer burden, D7 local/long-distance return, explicit AD Pro vs Max boundaries, supervision/rest, rental uncertainty, and official source links. These are content/render checks, not vehicle testing or browser interaction QA.
- The new/changed map and experience components pass targeted Oxlint. A broader page check still reports existing Next image/internal-link and React-compiler/style diagnostics in `app/page.tsx`; these do not prevent TypeScript or the static build. Local PNG `<img>` use is intentionally retained for static Leaflet marker markup without an image-optimization server.
- Static export succeeded with a temporary loopback port for Vinext prerendering. Non-blocking warnings remain for unused Leaflet default image assets and a client chunk over 500 kB. Local preview returned HTTP 200; this iteration was not deployed to the existing private site.
- Daily-image/leaf-colour change: `node scripts/check-day-visuals.mjs` checks 7 distinct sourced images, a photo for each day, historical labels, attribution, image-error isolation by React key, and 7 regional colour settings that return toward pale gold after the forest days. Green-shifted hues are now capped in response to user feedback. The submitted browser-comment screenshots informed these changes; no extra browser UI QA was requested or performed.
- Literary/leaf timeline addition: `node scripts/check-literary.mjs` checks the single short sourced excerpt, 3 scene PNGs (1536×1024), birch PNG (2172×724), route targets, original-text/AI-art disclosures, and non-looping reduced-motion styles. These are data/source checks, not browser interaction QA. All four new images were visually inspected; branch alpha verified by the asset agent.
- `node scripts/check-data.mjs`: 17 valid segments, 4 endpoint/overnight combinations, BD09 navigation URL encoding, WGS84 coordinate bounds, playback start/end interpolation, and exclusion of the erroneous hot-spring origin.
- Required Arxan photo returned HTTP 200 image/jpeg. OSM connectivity checked with the documented application-identifying User-Agent and actual local Referer; returned genuine cacheable image/png. Generic curl requests are not an appropriate OSM availability test.
- Generated leaf was inspected; genuine transparent RGBA PNG. Leaf motion is triggered only by clicks/scrolling (1.8–2.1 seconds), never autoplay or looping; capped and throttled particles are removed after finishing. Can be disabled, respects reduced motion, and does not intercept pointers. Photo is historical; leaf is decorative generated imagery.
- `configure_trip_preview` WebMCP registration is feature-detected and has input validation/AbortSignal cleanup. No supported live WebMCP validation context was available; its browser contract was not verified and is not required by the user.
- The supplied starter's package audit reports server/development dependencies with advisories. The delivered artifact is a static export only (`dist/client`); it exposes no server functions, image parser service, upload endpoint or development server. Dependencies are not silently force-upgraded. Reassess and patch before adding a public application server.
- Known scope limits: actual rental branches and hotels pending, navigation opens per leg (Baidu Web URI has no documented waypoint field), timing is a 2026-09-09 baseline and not holiday traffic prediction. Basemap availability depends on network/service access.
