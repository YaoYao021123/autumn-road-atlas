# Validation notes — 2026-09-09

## Latest feedback — detailed map and regional foliage

- Ultrawide screenshot follow-up: bounded the shared atlas to 1480 CSS px; routes use 12 px white halo, 9 px dark-blue casing, 6 px blue core and 6 px gray playback overlay. Scenic dashed corridors use a separate lower pane, so they cannot paint over the navigation line. Moved overlapping basemap/scenery controls apart. Explicit non-live-traffic legend; no invented congestion classes. Width/colour/layer-order source guards added to the map-delivery check.

- Restored detailed OSM tiles as the default; removed the grayscale/brightness filter that suppressed text contrast. Leaflet `detectRetina` uses higher-zoom tiles on high-DPI displays; `updateWhenIdle` delays requests during continuous panning. Local physical water context remains as a fallback, not a substitute for map detail. Higher pixel density costs additional tile requests; no faster-network claim is made.
- Four original motif families: poplar, existing birch, steppe grass and larch needles. Seven day assignments plus place-specific stop assignments, map/list markers and interaction leaves are covered by `check-regional-foliage.mjs`; transparency and per-asset byte budgets checked. Shape choices express regional landscapes, not local endemism or precise botanical identification. No continuous falling animation added.
- Three new generated images visually inspected individually before integration; 320 px transparent WebP exports retained. Existing routes, navigation coordinates, default D1, static forest scene and cultural content unchanged. No browser visual or interaction QA requested or performed.

## 2026-09-10 — publication/performance revision (supersedes earlier local-only notes)

- Probed the actual GitHub Pages and ChatGPT Site HTML and selected resources with curl. Both were stale D4 builds using the 2,426,541-byte Wikimedia source and old PNG artwork. Single samples: GitHub HTML 0.80 s, Site HTML 2.97 s, Wikimedia original 2.80 s, one OSM tile 0.75 s. This isolates multiple request chains; it does **not** prove all-region latency, browser LCP or a service outage.
- A read-only publishing review found GitHub prefix rewriting missed Rolldown backtick image URLs and escaped RSC font URLs. Extended matching, made the `_next` rewrite idempotent, and added final-output assertions for assets, font, dynamic dependencies, preload helper and `.nojekyll`.
- Original route JSON remains unchanged. Precision-6 delta encoding reduces source payload from 551,410 to 134,288 bytes; `check-data.mjs` checks every coordinate, all segment metadata, reproducibility and malformed input. All four airport/west/overnight variants retain their existing times, distances and scenic anchors. Initial page chunk is about 405 KB, down from the previous roughly 811 KB, before transport compression.
- Map starts with 5,992 bytes of local Natural Earth physical water context; no external tiles until the detailed-map toggle is selected. Source/data checks validate geometries, opt-in behavior, layer/listener cleanup and slow/error feedback. Approximate river/lake context is explicitly not a detailed street/navigation map. No map-tile proxy, prefetch, offline tile cache or undocumented provider endpoint is used.
- D4 image: 2,426,541 → 42,922 bytes, resized/re-encoded with original mayanming attribution and CC BY-SA 3.0. Film: inspected real Rediance still 7360×4912 showing mountains, distant passengers and coach, then exported 1440×961 WebP at 39,056 bytes. Embedded as an attributed film-discussion image with no open-license assertion or Manzhouli-location claim; no video player. Forest section stays static.
- TypeScript, route/day/scenery/culture/driving/static-forest checks and map-delivery source/data checks pass. Desktop/mobile film rules were examined in source, not in an automated browser. No new browser screenshot, interaction test or performance score is claimed.

## 2026-09-10 update

- Default day is D1. Font subset and optimized image paths are local. Corrected the invalid five-digit mask color and gave the photography scene two non-overlapping minmax tracks plus a real gap; image caption sits outside the opaque photo. Poster-inspired display type uses a licensed 36,976-byte subset rather than a complete multi-megabyte Chinese font. These are source/render checks guided by user screenshots, not a new browser visual audit.
- The forest scene was first explored as a two-layer car animation, then simplified to a **static background** at the user's explicit request. No car, playback controls, RAF, motion observer, or forest animation path remains in the rendered component. Original generated SUV asset is retained but not loaded. Mobile background 165,600 bytes; desktop 325,574 bytes; both lazy, async-decoded, low-priority WebP.
- Replaced browser-side `react-dom/server` use with safe DOM-node construction for Leaflet icons/popups. Removed the 187,377-byte server-renderer client chunk (56,734 bytes gzip in the previous build). Current-day view no longer draws all trip legs. Initial tiles wait for actual route bounds; a generation counter rebuilds route layers when Fast Refresh preserves React state but recreates the map.
- `check-forest-and-loading.mjs` performs an actual static forest React render, confirms no vehicle/player, tests all eight popup data trees with a small DOM-node model, malicious-text/URL handling, D1/HMR source contracts, valid mask colors, local font/license and image byte budgets. Original displayed local artworks are 6,258,374 → 467,496 bytes. No measured LCP, network waterfall or browser interaction claim is made.
- TypeScript and targeted new/changed-module Oxlint pass. Full repository Oxlint still reports existing starter-component accessibility/React-compiler and Next static-image rules; unrelated files were not rewritten. Static export and route/culture/driving/day-image checks pass. The latest film portrait is source-only. Existing private/public deployments are not changed by these local edits.

- Local page compiled and returned HTTP 200. Browser preview opened at the development server URL. Broad browser UI QA was not requested and was not performed.
- TypeScript checked with `npx tsc --noEmit`.
- Scenic-road and driving-effort addition: 8 mapped items (6 approximate window-view corridors, 1 optional parking entrance, 1 parking/transfer entrance). `node scripts/check-road-experience.mjs` passes all 4 endpoint/overnight combinations, exact parking anchors, no navigation for window markers, continuous selected-route highlights up to 5 km either side, an off-route guard, provenance, and input immutability. D6 follows the actual selected return leg. No routes were added or changed.
- `node scripts/check-driving-effort.mjs` performs 16 real React server renders and passes 9 groups of assertions: D1–D7 and overnight variants, 498/486/390-minute D6 baselines, D5 walking/transfer burden, D7 local/long-distance return, explicit AD Pro vs Max boundaries, supervision/rest, rental uncertainty, and official source links. These are content/render checks, not vehicle testing or browser interaction QA.
- The new/changed map and experience components pass targeted Oxlint. A broader page check still reports existing Next image/internal-link and React-compiler/style diagnostics in `app/page.tsx`; these do not prevent TypeScript or the static build. Local PNG `<img>` use is intentionally retained for static Leaflet marker markup without an image-optimization server.
- Static export succeeded with a temporary loopback port for Vinext prerendering. Non-blocking warnings remain for unused Leaflet default image assets and a client chunk over 500 kB. Local preview returned HTTP 200; this iteration was not deployed to the existing private site.
- Daily-image/leaf-colour change: `node scripts/check-day-visuals.mjs` checks 7 distinct sourced images, a photo for each day, historical labels, attribution, image-error isolation by React key, and 7 regional colour settings that return toward pale gold after the forest days. Green-shifted hues are now capped in response to user feedback. The submitted browser-comment screenshots informed these changes; no extra browser UI QA was requested or performed.
- Cultural-story revision: `node scripts/check-literary.mjs` checks 5 sourced works across 4 chapter anchors, geography/filming/story distinctions, source-only photography, one short excerpt, and both motion settings with real React server renders including the existing Button component. It also preserves the 3 original scene PNGs (1536×1024) and birch PNG (2172×724), and checks media links, disclosure and reduced-motion CSS. The old boxed tab-gallery contract was replaced with the requested continuous reading sequence. No route/parking data changed.
- Culture imagery: reused the original river and forest AI assets plus the historical Changchun photo. The film-image research agent verified the Arsenal/Berlinale still actually loads at 1074×670 and inspected the asset. No third-party files were downloaded. Wang Wei's photographer images are source links only, with the adjacent AI forest explicitly distinguished. The film trailer and CCTV report are links, not hidden players; external access remains platform-dependent. Layout verification is code/render based, not browser screenshots or interaction testing.
- `node scripts/check-data.mjs`: 17 valid segments, 4 endpoint/overnight combinations, BD09 navigation URL encoding, WGS84 coordinate bounds, playback start/end interpolation, and exclusion of the erroneous hot-spring origin.
- Required Arxan photo returned HTTP 200 image/jpeg. OSM connectivity checked with the documented application-identifying User-Agent and actual local Referer; returned genuine cacheable image/png. Generic curl requests are not an appropriate OSM availability test.
- Generated leaf was inspected; genuine transparent RGBA PNG. Leaf motion is triggered only by clicks/scrolling (1.8–2.1 seconds), never autoplay or looping; capped and throttled particles are removed after finishing. Can be disabled, respects reduced motion, and does not intercept pointers. Photo is historical; leaf is decorative generated imagery.
- `configure_trip_preview` WebMCP registration is feature-detected and has input validation/AbortSignal cleanup. No supported live WebMCP validation context was available; its browser contract was not verified and is not required by the user.
- The supplied starter's package audit reports server/development dependencies with advisories. The delivered artifact is a static export only (`dist/client`); it exposes no server functions, image parser service, upload endpoint or development server. Dependencies are not silently force-upgraded. Reassess and patch before adding a public application server.
- Known scope limits: actual rental branches and hotels pending, navigation opens per leg (Baidu Web URI has no documented waypoint field), timing is a 2026-09-09 baseline and not holiday traffic prediction. Basemap availability depends on network/service access.
